# Supabase local development configuration
# https://supabase.com/docs/guides/cli/config
#
# The project_id should match the linked remote project's ref (replace before
# running `supabase link`). All other defaults are kept conservative so a
# `supabase start` works out of the box for offline migration testing.

project_id = "nexus-editorial"

[api]
enabled = true
port = 54321
schemas = ["public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
shadow_port = 54320
major_version = 15

[db.pooler]
enabled = false

[studio]
enabled = true
port = 54323

[inbucket]
enabled = true
port = 54324

[storage]
enabled = false

[auth]
enabled = false

[edge_runtime]
enabled = false

[analytics]
enabled = false
